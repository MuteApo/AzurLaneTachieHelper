name: alth
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - numpy
      - pillow
      - pyqtdarktheme
      - pyside6
      - pytoshop
      - rich
      - six
      - tqdm
      - typing_extensions
      - unitypy
